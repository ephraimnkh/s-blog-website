{
  "name": "s-blog-website",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "install-all": "npm install && npm --prefix ./frontend install && npm --prefix ./backend install",
    "clean-setup-db": "npm --prefix ./backend run clean-setup-db",
    "setup-db": "npm --prefix ./backend run setup-db",
    "dev": "npm --prefix ./frontend run dev && npm --prefix ./backend run start:dev"
  },
  "author": "Ephraim Nkhoma",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
